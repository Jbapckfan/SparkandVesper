import SpriteKit

// Simplified shaders - removed for now to avoid compilation errors
// We're using SKShapeNode with blend modes for glow effects instead
enum Shaders {
    // Placeholder for future shader implementations
}